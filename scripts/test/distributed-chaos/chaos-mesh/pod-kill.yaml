apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: rustfs-pod-kill
  namespace: rustfs-chaos
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - rustfs-chaos
    labelSelectors:
      app.kubernetes.io/name: rustfs
